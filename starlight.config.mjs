import { defineConfig } from "starlight/config";

export default defineConfig({
  title: "Ma Documentation",
  defaultLocale: "fr",
  locales: {
    fr: {
      label: "Français",
      lang: "fr",
    }
  }
});